pipeline {
  agent any 
  stages {
     stage('Build') {
       steps { 
          sh 'docker build -t nilotpalgure1998/dvga:latest .'
       }
     }
     
     stage('Scan') {
     	steps {
	    sh 'trivy --security-checks vuln,config image nilotpalgure1998/dvga:latest'
        }
     }
  }
}
